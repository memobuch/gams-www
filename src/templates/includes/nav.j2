<nav class="navbar bd-navbar navbar-expand-lg mb-3 sticky-top bg-light border border-3 border-start-0 border-end-0">
  <div class="container-fluid px-4">
    {# <a class="navbar-brand" href="{{context._root_path}}" style="text-transform:uppercase;">{{context.project.projectAbbr
      }}</a> #}
    <button class="navbar-toggler text-light" type="button" data-bs-toggle="collapse"
      data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false"
      aria-label="Toggle navigation">
      <span class="navbar-toggler-icon text-light"></span>
    </button> 
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a data-active-template="project" class="nav-link" aria-current="page" href="{{context._root_path}}">{{context.project.projectAbbr | upper}}</a>
        </li>
        <li class="nav-item">
          <a data-i18n="nav.map" data-active-template="pages/map" class="nav-link" aria-current="page" href="{{context._root_path}}/map.html">Karte der letzten Wohnorte</a>
        </li>
        {# <li class="nav-item">
          <a data-i18n="nav.map_deaths" data-active-template="pages/map" class="nav-link disabled" aria-current="page" href="{{context._root_path}}/map_deaths.html">Karte der Todesorte</a>
        </li> #}
        <li class="nav-item">
          <a data-i18n="nav.biographies" data-active-template="object-list" class="nav-link" aria-current="page" href="{{context._root_path}}/objects/">Biographien</a>
        </li>
        {# <li class="nav-item">
          <a data-i18n="nav.places" data-active-template="object-list" class="nav-link disabled" aria-current="page" href="{{context._root_path}}/objects/">Erinnerungsorte</a>
        </li> #}
        <li class="nav-item dropdown">
          <a data-i18n="nav.project" class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Projekt
          </a>
          <ul class="dropdown-menu">
            <li><a data-i18n="nav.about" class="dropdown-item" href="{{context._root_path}}/about.html">About</a></li>
            <li><a data-i18n="nav.imprint" class="dropdown-item" href="{{context._root_path}}/imprint.html">Impressum</a></li>
            <li><a data-i18n="data_protection.title" class="dropdown-item" href="{{context._root_path}}/data_protection.html">Datenschutz</a></li>
            <li><a data-i18n="help.title" class="dropdown-item" href="{{context._root_path}}/help.html">Hilfe &amp; Rückmeldungen</a></li>
            <li>
              <hr class="dropdown-divider">
            </li>
            <li><a data-i18n="nav.datamodel" class="dropdown-item" href="{{context._root_path}}/datamodel.html">Forschungsdaten &amp; Datenmodell</a></li>
            <li><a data-i18n="nav.ontology" class="dropdown-item" href="{{context._root_path}}/ontology.html">Ontologie</a></li>
          </ul>
        </li>
         {# <li class="nav-item">
          <a data-i18n="nav.analysis" data-active-template="pages/geobrowser" class="nav-link disabled" aria-current="page" href="{{context._root_path}}/geobrowser.html">Analyse</a>
        </li> #}
      </ul>
      <ul class="navbar-nav me-2">
        <li class="nav-item">
          <a class="nav-link lang-switch" data-lang="de" href="#">DE</a>
        </li>
        <li class="nav-item">
          <a class="nav-link lang-switch" data-lang="en" href="#">EN</a>
        </li>
      </ul>
      <div class="d-flex" role="search" method="get" action="{{context._root_path}}/search.html">
        <a data-i18n="nav.search" class="btn btn-outline-dark" href="{{context._root_path}}/objects/" disabled>Suche</a>
      </div>
    </div>
  </div>
</nav>

<script>

  // set active nav link
  (() => {
    let selector = `nav a[data-active-template='${gams_frog._template_name}']`
    let navLink = document.querySelector(selector);

    // Debugging information only if in development using jinja2 conditionals
    {% if "dev" in context.env.GAMS_FROG_MODE %}
      console.debug("Setting active nav a element to selection: ", selector, " Selected element: ", navLink);
    {% endif %}

    if(!navLink){
      return;
    }
    document.querySelector(selector).classList.add("active");
  })();

  // set active language
  (() => {
    const currentLang = new URLSearchParams(window.location.search).get('lang') 
                        || '{{ defaultISOLanguage | default("de") }}';
    
    document.querySelectorAll('.lang-switch').forEach(link => {
      const targetLang = link.dataset.lang;
      
      // build href preserving current path and other params
      const params = new URLSearchParams(window.location.search);
      params.set('lang', targetLang);
      link.href = window.location.pathname + '?' + params.toString();
      
      // mark the active language
      if (targetLang === currentLang) {
        link.classList.add('active', 'fw-bold');
        link.setAttribute('aria-current', 'true');
      }
    });
  })();
</script>